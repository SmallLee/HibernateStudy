持久化对象的状态：
    Hibernate把对象分为四种状态：持久化状态，临时状态，游离状态，删除状态，Session可以转化这些状态

    临时状态：不处于Session缓存中，也可以说，不予任何一个session关联
    持久化状态：总是和一个session关联，与数据库中的记录对应，Session清理缓存的时候，会根据持久化对象的改变，更新数据库
    游离状态：Session断开关联，有OID
    删除对象：在数据库中没有记录，不再处于session缓存中